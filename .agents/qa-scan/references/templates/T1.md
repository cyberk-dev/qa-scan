# T1 — Pre-flight Failure

**Trigger:** `qa-orchestrator` Step -1 preflight check fail (Linear / GitNexus / Playwright MCP down).

**Diagnosis keys:** `mcp_name`, `tool_check_result`, `api_key_present`, `network_reachable`, `process_running`.

## Question payload (`escalation.question`)

> Pre-flight check thất bại cho `{mcp_name}`. Bạn muốn xử lý thế nào?

## Options (`escalation.options`)

| id            | label (VI)                  | description                                                       |
|---------------|-----------------------------|-------------------------------------------------------------------|
| fix_manual    | Sửa thủ công (Khuyến nghị)  | Tôi tự fix theo chẩn đoán ở trên, sau đó retry scan               |
| input_manual  | Nhập dữ liệu thủ công       | Bỏ qua MCP, paste nội dung issue trực tiếp                        |
| abort         | Huỷ scan                    | Stop pipeline, VERDICT=ABORTED                                    |

## Diagnosis block (rendered above the prompt)

```markdown
## ⚠️ Pre-flight Check Failed: {mcp_name}

### Chẩn Đoán
| Mục              | Kết quả | Ghi chú      |
|------------------|---------|--------------|
| MCP responding   | {ok}    | {detail}     |
| API key present  | {ok}    | {detail}     |
| Network reachable| {ok}    | {detail}     |
| Process running  | {ok}    | {detail}     |

**Nguyên Nhân Gốc:** {root_cause}

### Đề Xuất Sửa
```bash
{fix_command}
```
```

## Bash render hint

```bash
render_template T1 \
  --mcp_name "$MCP" \
  --root_cause "$ROOT_CAUSE" \
  --fix_command "$FIX_CMD"
```
