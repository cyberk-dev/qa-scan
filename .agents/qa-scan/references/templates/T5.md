# T5 — Test Generation Invalid

**Trigger:** `qa-test-generator` produces an unparseable spec / missing assertions / invalid selectors.

## Question payload

> Test file sinh ra không hợp lệ: `{reason}`. Bạn muốn xử lý thế nào?

## Options

| id            | label (VI)                          | description                                                          |
|---------------|-------------------------------------|----------------------------------------------------------------------|
| retry_context | Retry với context khác (Khuyến nghị)| Cung cấp thêm code context / test example, regenerate                 |
| manual_test   | Tôi viết test thủ công              | Paste test Playwright vào `evidence/{issue}/test.spec.ts`             |
| skip          | Bỏ qua test generation              | Verdict = PARTIAL, manual verify required                             |
