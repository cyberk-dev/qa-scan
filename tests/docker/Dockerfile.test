FROM ubuntu:22.04

ENV DEBIAN_FRONTEND=noninteractive

RUN apt-get update && apt-get install -y \
    curl \
    ca-certificates \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /app
COPY install.sh .

CMD ["bash", "-c", "bash install.sh --non-interactive --dir /tmp/qa-scan"]
